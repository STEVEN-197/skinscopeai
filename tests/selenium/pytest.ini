[pytest]
testpaths = tests/selenium/scripts
addopts = -v --tb=short --html=tests/reports/selenium_report.html --self-contained-html
markers =
    smoke: critical-path smoke tests
    auth: requires real credentials
